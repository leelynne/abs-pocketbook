# PocketBook SDK (large, fetched by scripts/setup-sdk.sh)
sdk/

# Build output
build/
*.o
*.app

# ...except the published binary, so the app can be installed without a build
!release/ABSClient.app

# Snapshots of device firmware config taken before edits
device-backup/

# Editor / OS
.DS_Store
.#*
*~
