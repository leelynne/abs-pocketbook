# PocketBook SDK (large, fetched by scripts/setup-sdk.sh)
sdk/

# Build output
build/
*.o
*.app

# Built by scripts/release.sh and attached to a GitHub Release, not committed:
# a binary in the tree silently drifts from its source, and one shipped here
# once carried a fixed-in-source security bug for exactly that reason.
release/

# Snapshots of device firmware config taken before edits
device-backup/

# Editor / OS
.DS_Store
.#*
*~
corpus/
